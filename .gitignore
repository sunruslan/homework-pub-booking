# ── Secrets ──────────────────────────────────────────────────────────
# NEVER commit .env — it has your API keys.
.env
.env.local
.env.*.local

# ── Session artifacts ────────────────────────────────────────────────
# Ignore sessions by default; Ex9 reflection cites specific dirs (tracked below).
sessions/*
!sessions/examples/
sessions/examples/*
!sessions/examples/ex5-edinburgh-research/
sessions/examples/ex5-edinburgh-research/*
!sessions/examples/ex5-edinburgh-research/sess_635e16ae529d/
!sessions/examples/ex5-edinburgh-research/sess_635e16ae529d/**
!sessions/examples/ex7-handoff-bridge/
sessions/examples/ex7-handoff-bridge/*
!sessions/examples/ex7-handoff-bridge/sess_710ea14ce8d1/
!sessions/examples/ex7-handoff-bridge/sess_710ea14ce8d1/**
!sessions/examples/ex7-handoff-bridge/sess_fae375db36bd/
!sessions/examples/ex7-handoff-bridge/sess_fae375db36bd/**
!sessions/examples/ex7-handoff-bridge/sess_23ce0baaa5ca/
!sessions/examples/ex7-handoff-bridge/sess_23ce0baaa5ca/**
demo_sessions_*/
*.session.json

# ── Python standard ──────────────────────────────────────────────────
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.venv/
venv/
ENV/
env.bak/
.pytest_cache/
.ruff_cache/
.mypy_cache/
.tox/
*.egg-info/
build/
dist/
.coverage
htmlcov/

# ── Rasa ─────────────────────────────────────────────────────────────
# Generated models, should not be in source control.
rasa_project/models/
rasa_project/.rasa/
rasa_project/results/

# ── OS ───────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
desktop.ini

# ── Editors ──────────────────────────────────────────────────────────
.vscode/
.idea/
*.swp
*~

# ── Answers held locally but not submitted ───────────────────────────
# If you keep working notes, put them under answers-local/ instead.
answers-local/

# ── Uploads for grader ───────────────────────────────────────────────
# The grader's private tests are materialised here at CI time.
# They should never be checked in by students.
tests/private/*
!tests/private/.gitkeep

# ── uv ───────────────────────────────────────────────────────────────
# uv.lock IS committed on purpose (reproducible installs). Do not add it here.

# ── Educator solutions (NEVER COMMIT) ────────────────────────────────
# The `solution/` directory contains reference implementations of every
# exercise. It lives in the educator's private workspace only. Students
# never see it.
#
# If you're a student looking at .gitignore: `solution/` does not exist
# in the public repo. We generate it on the educator's machine from a
# sibling private repo (homework-pub-booking-solutions) that isn't open.
solution/
educator/_scratch/
